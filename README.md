# django-djoser-cookie-auth [WIP]

This is not a production-ready code. It's just first try to implement DRF JWT auth through cookie, using djoser. I still have problems with normal token refreshing.
